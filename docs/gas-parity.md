# WordPress / GAS 共通版の更新と対応状況

更新日: 2026-09-07。共通公開版: 0.6.132。公開と実接続の確認は下記の記録を参照してください。

## 正本

正本は、このファイルがある `Voice-Casting-Studio` リポジトリです。
WordPress版とGAS版は `src/main.jsx`、`src/components`、`src/lib/recording.js` を共用します。
GASの声優向けURLも同じAppを表示します。以前の簡易声優画面を別途改修しないでください。

旧Claude作業フォルダーには `CLAUDE.md` / `AGENTS.md` で正本への案内を設置しました。
旧フォルダーのアプリ自体は上書きしていません。

## 対応状況

| 項目 | 今回の対応 | 検証範囲 |
| --- | --- | --- |
| ホーム、コンセプト・ビジョン、台本、キャラクター、共有リンク、素材、質問、タスク、予定、マニュアル | 現行プラグイン版の画面・部品を共用 | ローカルのGASテスト受信口とChrome |
| 声優の収録チェック・章内まとめ操作 | 担当キャラクターだけ保存可能。本文から展開したセリフにも対応 | 単体テスト、ブラウザで保存と再読込 |
| 制作者の確認・リテイクと声優の収録状態 | 項目ごとの更新時刻で統合し、片方の操作でもう片方を巻き戻さない | 実際のCode.gsを使うメモリー内テスト |
| 質問、回答済みの解決、追加質問 | GAS保存に接続。質問者を共有キーから判定 | 単体テスト、ブラウザで投稿・解決 |
| キャラクターのSNS・収録フォルダー・注意点 | 共通画面で表示 | ブラウザ |
| 素材プレイヤー、タブ切り替え | 共通の追従プレイヤーを使用 | テスト音の再生とタブ移動 |
| 制作者による共有内容の更新 | 公開済み作品を画面に関係なく遅延保存。最新の進捗を統合 | 保存処理と統合ロジックのテスト。実GASへの連続編集は公開後に要確認 |
| GAS制作者の非公開制作データ全体のクラウド保存 | protocol 3で追加。本人のMy Drive内に非公開JSONを保存。空の場合だけ従来の端末データを移行 | 単体テストと実クラウド読戻し。既存3作品・台本303行・人物・共有キーを照合済み |
| フォーム複製・期限・応募者取込 | 共通サービス窓口からGASへ接続。見本フォームと保存先は導入者が指定 | 追加権限承認済み。実フォームの複製・期限・構造・再読込・重複防止を確認。応募者取込はサーバー契約テスト |
| OpenAI画像生成・最終サイズ調整・画像監査 | GASが生成、ブラウザーが指定サイズに調整、GASが監査して合格画像だけDriveへ保存 | 不正画像拒否と保存経路を単体テスト。実画像生成の課金テストは未実施 |
| ElevenLabs生成・利用枠・キー設定 | GAS側に追加。450文字制限、確認必須、APIキーを声優レスポンスへ返さない | モックAPIによる生成・課金量集計・キー分離テスト。実生成は未実施 |
| Googleフォームのテーマヘッダー設定・音声アップロード先復旧、X画像添付 | 既存のPC仕上げ補助プログラムを共用。GASの公開元を許可リストへ追加 | 最新版を起動しGAS公開元からのhealth/CORS応答を確認。実際の仕上げ・X添付は未検証で、対象Chromeのログインが必要 |
| WordPress本番 / 実際のGASへの公開 | GAS受信口バージョン6、GitHub PagesとWordPressの0.6.132を公開済み | 両公開先のソース指紋一致。声優2名とオーナーの16項目、およびオーナーフォームの13項目を実接続で検証 |

「同じ画面」「サーバー実装済み」「本番の外部連携確認済み」は分けて報告します。権限承認・API設定・実動作が未確認の機能を、すべて完了したと表示しないでください。

## 配布時のAPIと保存先

- 配布者のOpenAI/ElevenLabs APIキーは同梱しません。制作チームごとに、利用するオーナーが自分のキーを設定します。
- GAS版は制作オーナーごとにApps Scriptをコピーし、そのオーナーとしてデプロイします。別の制作オーナーと同じバックエンドを共用しません。
- APIキーはGASのスクリプトプロパティへ保存します。制作データJSON・共有レスポンス・画面の再表示へ含めません。Apps Scriptの編集者からは閲覧できるため、声優や他の制作オーナーへ編集権限を付けません。
- 共同収録の同期トークンと声優の共有キーは別です。同期トークンを知る人は制作オーナーとして操作できるため、声優へ配らないでください。
- 見本フォーム、作成先フォルダー、画像フォルダー、ロゴ、参考URLも各オーナーが設定します。設定なしで配布者のAPIや保存先へ自動的に切り替わる処理はありません。
- 共通UIの既存サンプル文章にはUmbrella Paradeの例が含まれます。別作品へ導入するときは作品名・台本・テンプレート・共有先を本人のものへ設定します。
- 初期設定には `artifacts/paired-build/gas-backend/Code.gs` と `appsscript.json` の両方を使用します。Apps Scriptで `authorizeVoiceCastStudio` を実行し、必要なGoogle権限を本人が承認します。
- Drive・フォーム・スプレッドシートの操作、外部APIへの送信、募集締切のトリガーに権限を使用します。APIの有料生成はオーナーの操作時のみ実行します。

## 共有と権限

- 共有プロトコルは `2`。サーバー側 `Code.gs` とフロントを一緒に更新する必要があります。
- GASに保存する共有用データは許可した項目だけです。オーナー用APIキー、応募者の個人情報、アクセスキー一覧、非公開タスクの設定などを声優向けレスポンスへ含めません。
- 本文から展開したセリフの担当者は、オーナーが公開した `recordingLineIndex` で検証します。声優側が送信する担当者情報を信用しません。
- 更新はロック下で保存します。一括更新は全件を検証してからファイルに書き込みます。
- 質問の削除は削除IDも共有して、古い公開内容による復活を防ぎます。
- フィールドごとの時刻による競合解決のため、端末時計の大きなずれには注意が必要です。

## 以後の更新

1. 正本の共通ソースを変更します。WordPress向けとGAS向けで同じ画面を複製しません。
2. 保存・外部APIに関わる変更は、`src/lib/workspace.js`、`src/lib/services.js` と `docs/google-apps-script` の受信口・OwnerBackend・OwnerAuditionを確認します。
3. `npm run build:all` を実行します。全テスト、GASビルド、WordPressビルドを順に実行し、共通ソースの指紋を記録します。
4. GASフロントは `dist/`、WordPress候補は `artifacts/paired-build/wordpress/voice-casting-studio/`、GAS受信口とマニフェストは `artifacts/paired-build/gas-backend/` に出力されます。分割元のCode.gsだけをデプロイしないでください。
5. 各 `build-info.json` のバージョンと `sourceSha256` が一致することを確認します。
6. WordPressとGASフロントをそれぞれ公開します。GASサーバー変更を伴う場合はApps Scriptも新しいバージョンとして再デプロイします。
7. 制作者と声優2名のテストデータで、保存、再読込、担当者制限、リテイク、質問、素材再生を確認します。

GitHub ActionsのPagesワークフローも `build:all` を呼ぶよう変更しています。
公開の完了はGitHub Actionsの成功と公開先の `build-info.json` で確認します。
CIはApps Scriptの本番デプロイを自動では行いません。

## 実GASでの検証記録

### 0.6.132

公開画面: https://umbrellaparade.github.io/Voice-Casting-Studio/?v=0.6.132

公開コミット: `f507dd45bfdf689e550fcb17f9a08abf46e20a88`。
GitHub Actions実行 `34028553463` が成功しました。共通テスト179件と両版ビルドを実行しています。
公開ソース指紋: `b02a48244e43ebd21548009240503b125a25084a59b79630ac6d461a08c33a13`。

GAS受信口は2026-09-06にバージョン6で公開済みです。Drive・フォーム・スプレッドシート・外部接続・トリガーの明示スコープを本人が承認しました。
見本フォーム、フォーム保存先、画像保存先、ロゴ、オーディオブックURLを設定し、再デプロイ後も保持されることを確認しました。
OpenAI/ElevenLabsの実キーはGASへまだ登録していません。有料生成は実行していません。

オーナーの従来端末データを退避してからクラウド保存へ移行しました。3作品、303件の台本行、人物情報、配役の共有キーが一致することをクラウドからの読戻しで確認しました。
別の検証用作品 `vcs_verification_2964955df194` で、収録共有・担当者制限・リテイク更新・質問の解決など16項目に合格しています。
これらのバックアップは公開リポジトリの外の `private-backups` に保存しました。

2026-09-07に `tools/verify-gas-owner-live.mjs` で追加13項目に合格しました。設定保持、匿名アクセス拒否、秘密値を返さないこと、隔離したテスト作品の保存、フォーム複製・構造検査、URL保存、再読込、再実行時の重複防止、既存制作データの保持を含みます。
検証フォームは過去の締切を指定し、回答受付が閉じた状態にしました。検証作品だけを制作データから取り除き、既存作品が変化していないことを照合しました。検証フォーム自体は閉じた状態で保存先へ残しています。
API側だけではテーマヘッダー設定・音声アップロード先の動作を確認できないため、未確認フラグが誤って完了にならないことも検証しました。

PC補助プログラムは2026-09-07に最新版で起動し、GAS公開元をOriginにした `/health` がHTTP 200と一致するCORS許可を返すことを確認しました。PCフォーム仕上げ・X画像添付の実操作は未確認です。

WordPress本番も2026-09-07に0.6.130から0.6.132へ更新しました。更新前にAll-in-One WP Migrationで935,455,685バイトのフルバックアップを作成し、ローカルへ保存しました。8,397エントリーすべてのCRC32とアーカイブ全体を検査し、データベースから非公開制作データ3作品を抽出してJSONの読戻しに合格しています。これはバックアップの読戻し確認であり、本番への復元操作はしていません。
更新後の本番画面は839セリフ・収録済み443件・確認OK369件・リテイク1件でバックアップと一致し、制作オーナー権限と保存済み表示を確認しました。
WordPressとGASフロントの公開 `build-info.json` はともに0.6.132、ソース指紋 `b02a48244e43ebd21548009240503b125a25084a59b79630ac6d461a08c33a13` で一致しています。

WordPress本番の現在の制作データをGASへコピーする操作はしていません。GASは従来のテスト用制作データを維持し、両サイト間で制作データを自動同期する仕組みではありません。

### 0.6.131

公開画面: https://umbrellaparade.github.io/Voice-Casting-Studio/?v=0.6.131

公開コミット: `ef17ef93b88a7f756e0e11e58e74018fbdc5a666`。
GitHub Actions実行 `34022406274` が成功し、165件のテストと両版のビルドに合格しました。
公開先の `build-info.json` はローカル検証版と一致しています。
ソース指紋: `721d86b7744a3b25f3a18eb58cd52cfc1feb81691b523ef44b2cacfdf9e51eeb`。

公開画面からテスト声優Aが再収録チェックを保存し、再読み込みと別の声優Bの画面で保持・共有を確認しました。
既存の管理画面でも同期完了を確認し、同期後のDrive原本をもう一度取得して、303行の台本文・配役と共有キー・既存4件の派生進捗が保たれていることを照合しました。
WordPressの現在の制作データをGASへ移行する操作は行っていません。GASは従来のテスト用制作データを維持しています。

2026-09-06に、既存の「Voice Cast Studio 収録共有テスト」をバージョン3へ更新しました。
公開URL、実行ユーザー、公開範囲、秘密トークンは維持しています。

実作品とは別のテスト作品を使い、声優2名とオーナーによる16項目の実接続テストに合格しました。
収録チェック、担当外の更新拒否、確認状態の保護、一括更新、リテイク・再リテイク、再公開時の進捗保持、質問の投稿・回答・解決を含みます。

既存作品には担当者検証用の索引だけを追加しました。303件の保存行、キャラクター、配役・共有キー、既存の進捗を、更新前後のDriveファイルで照合済みです。
実データ、共有キー、秘密トークンを含むバックアップと検証レポートは公開リポジトリの外に保存し、GitHubへ含めません。

## 既存GASの更新順序

1. Apps Script編集URL、現在のデプロイ、共有保存先を確認し、既存JSONを日時付きでバックアップします。
2. 既存の保存先ID、共有キー、秘密トークンを保持して受信口を更新します。秘密情報をGitへ記録しません。
3. テスト用保存先でプロトコル2の動作を確認します。
4. 本番受信口を再デプロイし、共通フロントも公開します。
5. オーナー画面の「配役・共有」から既存作品の共有内容を更新し、セリフの担当者索引を保存します。
6. 声優URLで最新版が見えることと、保存したチェックをオーナーが取得できることを確認します。

古い受信口に新しいフロントだけを接続すると、更新が必要である旨を表示します。
検証に成功していない状態を「共有済み」として扱わないでください。

## ローカル検証

`tools/gas-demo-server.mjs` は実際の `Code.gs` をメモリー内で動かすテスト受信口です。
Googleアカウント、実際のDrive、本番WordPress、課金APIには接続しません。終了するとテスト保存内容は消えます。
既存のClaudeプレビューと衝突しないよう共通フロントは5261、テスト受信口は5272を使用しています。

`test/gas-workspace.test.js` では、担当者制限、一括処理の原子性、古い応答、削除復活、公開時の競合統合、共有データの漏えい防止を検証します。
ブラウザ検証画像は `artifacts/gas-parity-2026-09-06/` に保存しています。
