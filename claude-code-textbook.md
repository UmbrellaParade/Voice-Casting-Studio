# Claude Code 教科書 —— ノウハウコレクターをやめるための本

読了めやす：約10分

> **読みたくない人へ**
> この文書を読まなくても構いません。`ai-company/` フォルダを解凍して、
> `materials/` に商材を入れて、`claude` と打つだけで動きます。
> 仕組みを知りたくなったときに、ここへ戻ってきてください。

---

## 1. なぜ買った商材が実行されないのか

情報が足りないからではありません。むしろ余っています。

実行されない理由はひとつです。

> **情報と実行のあいだに「人間の気力」が挟まっているから。**

商材を買う → 読む → 理解する → 自分の状況に翻訳する → 手順に分解する → 毎日やる。
この鎖のどこか一箇所で気力が切れると、すべて止まります。そして必ずどこかで切れます。

だから普通は「意志を強くしよう」という話になります。それが精神論です。効きません。

**この教科書がやるのは逆のことです。人間を鎖から外します。**

読む・理解する・翻訳する・分解する・毎日やる —— この5つを AI に渡します。
人間に残すのは、**買う**ことと、**最後に確認して世に出す**ことだけです。

---

## 2. Claude Code とは何か

ひとことで言うと、**あなたのパソコンの中で実際に手を動かす AI** です。

チャット型の AI との違いはここです。

| | チャット型AI | Claude Code |
|---|---|---|
| できること | 答えを返す | ファイルを読む・書く・作る・実行する |
| 商材を渡すと | 要約してくれる | 手順書に変換して**実行する** |
| 結果 | 画面に文字が出る | **あなたのフォルダに成果物が残る** |

「教えてくれる先生」ではなく「代わりに作業する部下」です。

---

## 3. 5分で入れる

ターミナル（Mac なら「ターミナル」、Windows なら「PowerShell」）を開いて、次を打ちます。

```bash
npm install -g @anthropic-ai/claude-code
```

`npm` が無いと言われたら、先に [Node.js](https://nodejs.org/) を入れてください。

入ったら確認します。

```bash
claude --version
```

バージョン番号が出れば完了です。

**デスクトップアプリ版もあります。** ターミナルが苦手なら、そちらでも同じことができます。

---

## 4. 最小の使い方

```bash
mkdir 作業フォルダ
cd 作業フォルダ
claude
```

これで AI がそのフォルダの中に入った状態になります。あとは日本語で頼むだけです。

重要なのは **フォルダ単位で動く** という点です。
AI が読めるのも、書けるのも、そのフォルダの中だけ。
だから「商材を入れるフォルダ」を1つ作れば、そこが作業場になります。

---

## 5. `CLAUDE.md` —— 毎回読ませる指示書

フォルダに `CLAUDE.md` という名前のファイルを置くと、
**セッション開始時に必ず読まれます。**

つまり、毎回同じことを説明し直す必要がなくなります。

たとえばこう書いておきます。

```markdown
# 指示

- 指示待ちをするな。次にやることは自分で判断して着手しろ。
- 精神論・抽象論は成果物に入れるな。
- 公開・投稿・送信は絶対に自動でやるな。下書きまでにしろ。
```

これが **起動スイッチ** です。
「毎回ちゃんと指示を出す」という気力が要らなくなります。

> Codex など他のツールを使う場合は、同じ内容を `AGENTS.md` という名前でも置いておきます。

---

## 6. Skill —— 商材を手順書に変える場所

ここがこのやり方の中心です。

`.claude/skills/<名前>/SKILL.md` というファイルを置くと、
AI は **必要な場面で自動的にそれを読みに行きます。**

ファイルの先頭にはこう書きます。

```markdown
---
name: blog-monetize
description: ブログ記事を書くとき、収益化の手順を実行するときに使う
---
```

この `description` が「どんな時に使うか」の判定材料になります。

### 商材は「読む物」ではなく「手順書」に変える

ここが一番の設計判断です。

商材をそのまま AI に読ませても、要約が返ってくるだけです。
そうではなく、**商材を `.claude/skills/` に置く実行手順書に変換します。**

変換のときに必ずやることが2つあります。

**(1) 各ステップに完了条件を書く**

```
✗ 「タイトルをいい感じに考える」
○ 「work/outputs/titles.md に、30文字以内の見出し案が10件ある状態」
```

「いい感じに」では AI は判定できません。判定できないと、実行が曖昧に終わります。
**目で見て確認できる形**にすると、AI は自分で「終わったか」を判断できます。

**(2) 抽象論・精神論は捨てる**

商材の分量の多くは、心構えと成功体験です。
実行には一切寄与しないので、変換の時点で落とします。

---

## 7. レビューを内側に持つ

作った手順書には必ず穴があります。書いた本人が読み返しても見つかりません。

そこで **AI 自身に査読させます。**

最初は「Claude Code で作って、別途チャットの Claude に貼ってレビューさせる」ことを考えました。
やめました。**Claude Code 自身が同じモデルなので、外に出す意味がない**からです。

外に出すと、コピペという人間の作業が発生します。それが気力を削ります。
だから **内側に閉じます。**

手段は3つ、どれか1つ使えれば十分です。

```bash
# 手段A：サブエージェントに査読させる（同じセッション内・最速）

# 手段B：別セッションを立てて査読させる（文脈が切れるので精度が高い）
claude -p "$(cat .claude/skills/blog-monetize/SKILL.md)
上の手順書を査読して、問題点だけ箇条書きで返して。なければ NO_ISSUES とだけ返して。"

# 手段C：Codex など別のツールに投げる
codex exec "..."
```

返ってきた指摘は **AI が自分で直します。** あなたには回ってきません。
最大3周で打ち切り、残った指摘は理由をつけて記録して先へ進みます。

---

## 8. 自動化する

一度やって終わるノウハウはほとんどありません。繰り返す部分だけ自動にします。

| やりたいこと | 使うもの |
|---|---|
| 毎日・毎週まわしたい | `/schedule`（PCを閉じても動きます） |
| 1回だけ流したい | `claude -p "..."` |
| 条件が揃うまで繰り返したい | `/loop` |

`/schedule` はクラウド側で動くので、**PCを閉じていても止まりません。**
「毎朝ネタを10個出しておく」「毎週データを集計しておく」が寝ている間に終わります。

### スマホから頼む

Dispatch を使うと、外出先のスマホから自宅の PC に作業を頼めます。
ただし **デスクトップアプリが起動していて、PC が起きている必要があります。**

---

## 9. やらせてはいけないこと

これは技術的な制限ではなく、**設計上の判断**です。

AI に自動でやらせないこと：

- SNS への投稿
- メール・DM の送信
- 記事・ページの公開
- 支払い・課金
- ファイルの削除

理由は単純で、**事故が起きたときに取り返しがつかない**からです。
消した投稿は消えたまま、送ったメールは戻りません。

だから **下書きの生成までを自動、最後に世に出す判断だけ人間** に残します。

これは「AI を信用していない」からではありません。
自動化の価値は、**人間がやらなくていい作業を消すこと**であって、
**人間が背負うべき責任まで渡すこと**ではないからです。

---

## 10. まとめ ——「読まなくても動く」の作り方

やってきたことを1枚にすると、こうなります。

```
商材（読んでない）
   ↓  フェーズ0：棚卸し           何ができる商材かを1文にする
   ↓  フェーズ1：手順書化          .claude/skills/ に変換。完了条件を必ず書く
   ↓  フェーズ1.5：自動レビュー    AI 自身に査読させ、AI 自身が直す（人手ゼロ）
   ↓  フェーズ2：実行             実際に手が動き、work/outputs/ に成果物が残る
   ↓  フェーズ3：自動化           繰り返す部分だけ /schedule に載せる
成果物
```

この5段を全部やってくれるのが、同梱の `ai-company/` キットです。

### 使い方はこれだけ

```bash
# 1. フォルダを置く（zip なら解凍する）
# 2. materials/ に商材を入れる（整理しなくていい）
# 3. そのフォルダで:
claude
```

`CLAUDE.md` が読まれ、スキルが読まれ、AI が自分でフェーズを判定して走り出します。
**あなたから指示を出す必要はありません。**

---

## 最後に

ノウハウコレクターは、意志が弱いのではありません。
**情報を実行に変える作業が、全部人間の側に置かれていた** だけです。

その作業を機械に渡せるようになりました。
だから、これからは「買ったけどやっていない」が成立しません。

積んである商材を1本、`materials/` に入れるところから始めてください。
